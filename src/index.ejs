<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>58COIN交易平台</title>
  <% if (htmlWebpackPlugin.options.nodeModules) { %>
  <!-- Add `node_modules/` to global paths so `require` works properly in development -->
  <script>
    require('module').globalPaths.push('<%= htmlWebpackPlugin.options.nodeModules.replace(/\\/g, '\\\\') %>')
  </script>
  <% } %>
  <script src="/static/js/jquery-3.2.1.min.js"></script>
  <script src="/static/js/zlib.pretty.js"></script>
  <script src="/static/js/lottie.min.js" type="text/javascript"></script>
  <script src="/static/js/BigDecimal-all-last.min.js"></script>
  <script src="/static/trading1.4/charting_library/charting_library.min.js"></script>
  <link rel="stylesheet" type="text/css" href="/src/renderer/assets/main.css">
</head>

<body>
  <div id="app"></div>
  <!-- Set `__static` path to static files in production -->
  <% if (!process.browser) { %>
  <script>
    if (process.env.NODE_ENV !== 'development') window.__static = require('path').join(__dirname, '/static').replace(
      /\\/g, '\\\\')
  </script>
  <% } %>

  <style>
  html,body,div,li,dd,p,a,frame,input,label,from {
    margin: 0;
    padding: 0;
}
  </style>

  <!-- webpack builds are automatically injected -->
</body>

</html>